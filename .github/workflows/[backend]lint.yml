name: Backend TypeCheck

on:
  push:
    branches: [ main ]
    paths:
      - 'backend/**'
  pull_request:
  workflow_dispatch:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies with uv
        working-directory: backend
        run: |
          python -m pip install --upgrade uv
          uv sync --group dev

      - name: Lint with Ruff
        working-directory: backend
        run: |
          uv run ruff check backend/dataforce_studio

      - name: Check formatting with Ruff
        working-directory: backend
        run: |
          uv run ruff format --check backend/dataforce_studio
